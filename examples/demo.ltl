// examples/demo.ltl — runnable demo for ltl_tetris

import std::io
use ltl_tetris as lib

fn main() {
    io::println("ltl_tetris demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}